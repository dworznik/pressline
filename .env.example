# Copy to .env for local development. Never commit .env.
# Secrets are read from the platform environment in production (ADR-0014).

PRINTFUL_TOKEN=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
RESEND_API_KEY=
OPERATOR_TOKEN=
SESSION_SECRET=
# One per Engine slug declared in pressline.config.ts, e.g. ENGINE_SECRET_SAMPLE=
