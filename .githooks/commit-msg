#!/usr/bin/env sh
# Conventional subject line, checked by commitlint (commitlint.config.js):
# type(scope)?: summary, <= 72 chars. Types: feat fix docs test refactor chore
# ci build perf adr. Merge/Revert/fixup!/squash! are ignored by config-conventional.
pnpm exec commitlint --edit "$1"
