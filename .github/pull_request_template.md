## What

<!-- One paragraph. Link the ticket: Closes #NN -->

## Checks

- [ ] Uses `CONTEXT.md` vocabulary; no `_Avoid_` terms introduced
- [ ] Respects the ADRs in `docs/adr/` (or adds/supersedes one)
- [ ] Tested at the highest seam (HTTP harness / contract fixtures / render headers / conformance)
- [ ] No secrets, no image bytes stored by the bridge, no `withTransaction`
